import SectionCatalogTop from "../components/SectionCatalogTop";

const Catalog = ()=>{
    return(
        <main className="main">
            <SectionCatalogTop/>
        </main>
    )
}

export default Catalog;